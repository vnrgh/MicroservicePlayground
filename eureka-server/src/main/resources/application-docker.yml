server:
  port: 8761

spring:
  application:
    name: eureka-server
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
management:
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans